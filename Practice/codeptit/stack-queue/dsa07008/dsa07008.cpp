#include <bits/stdc++.h>
using namespace std;
void testCase() {
    string s;
    cin >> s;
    stack<string> st;
    for (int i = 0; i < s.size(); i++) {
        string temp = string(1, s[i]);

    }
}
int main () {
    int t;
    cin >> t;
    while (t--) {
        testCase();
        cout << endl;
    }
}